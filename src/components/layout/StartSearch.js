import { Text, Center } from "native-base"

const StartSearch = () => {
  return (
    <Center>
      <Text fontSize="xl" mt={20} bold>Please initiate a search</Text>
    </Center>
  )
}

export default StartSearch